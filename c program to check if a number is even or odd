#include<stdio.h>
 int main(){
 int num;
printf("entere the integer: ");
scanF(%d,&num);
 f (number % 2 == 0) {
        printf("%d is even.\n", number);
    } else {
        printf("%d is odd.\n", number);
    }

    return 0;
}
